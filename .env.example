# Copy this file to .env and fill in real values. See src/lib/company-auth.client.ts
# and src/lib/portal-auth.server.ts for how each of these is used — note that in this
# branch, all outbound API calls (auth, pharmacy orders, payments, Supabase) are mocked
# and return canned data regardless of these values; only prescription OCR extraction
# still calls a real API.

VITE_COMPANY_API_BASE_URL=
COMPANY_API_BASE_URL=
PROVIDER_API_BASE_URL=

QWEN_BASE_URL=
QWEN_API_KEY=

SUPABASE_PROJECT_ID=
SUPABASE_PUBLISHABLE_KEY=
SUPABASE_URL=
VITE_SUPABASE_PROJECT_ID=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_SUPABASE_URL=
